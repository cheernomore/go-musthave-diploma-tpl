// Package config loads the gophermart service configuration from command-line
// flags and environment variables. Environment variables take precedence over
// flag values, matching the convention used by the project autotests.
package config
