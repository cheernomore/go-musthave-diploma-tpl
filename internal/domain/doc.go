// Package domain contains the core entities of the gophermart service —
// users, orders, balances and withdrawals — together with sentinel error
// values that are returned across layer boundaries via errors.Is.
package domain
