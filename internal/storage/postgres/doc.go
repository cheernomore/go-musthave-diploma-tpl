// Package postgres provides a PostgreSQL-backed storage layer for the
// gophermart service. It exposes a connection pool, embedded schema
// migrations, and repository implementations consumed by the service layer.
package postgres
