// Package service contains the business logic of the gophermart loyalty
// system. Sub-packages implement specific use cases (auth, orders, balance)
// and declare the repository interfaces they consume; concrete repository
// implementations live in internal/storage.
package service
